# Занятие 1 (27-05-25) Подготовка базы данных

Модуль 1 Проектирование и разработка информационной системы  
1.1 Спроектировать и создать БД  
- согласованную осмысленную схему именования - означает, что все таблицы и сама БД будут именоваться в терминах сущности предметной области   
- создайте необходимые первичные и внешние ключи  
первичный ключ - отметить PK AI  
внушний ключ - связь с другой таблицей (выбирается таблица и первичное поле этой таблицы)  
- ограничения внешних ключей (каскадное удаление CASCADE и т.д)  
- заполнить все таблицы тестовыми данными (не менее 3 записей в каждой таблице)  

Описание предметной области:  
- выставление оценок обучающимся по дисциплинам - это сущность "журнал" journal - назовем по англ так БД  

**Запускаем XAMPP + apache+mysql -> admin - открываем phpMyAdmin**  

Ищем сушности (существительные) в тексте описания:  
пользователи - users  
	к ним относятся (роли) преподаватели и студенты (2 роли)  
 ```
 iduser int PRIMARY KEY AUTO_INCREMENT
 login VARCHAR(100)
 role VARCHAR(10)	- teacher, student
 password VARCHAR(100)
 idgroup int - ид.группы для student
```
группы - groups и студенты в них (препод просматривает студентов в группе)  
```
 idgroup int PRIMARY KEY AUTO_INCREMENT
 name VARCHAR(100)
```
дисциплины (предмет) - subjects (студент смотрит оценки по дисциплинам)
```
 idsubject int PRIMARY KEY AUTO_INCREMENT
 name VARCHAR(100)
 idteacher int  - ид.преподавателя предмета
```
оценки - scores (препод доб-т, изм-т, удал-т)  
```
 idscore int PRIMARY KEY AUTO_INCREMENT
 date_score DATE
 iduser int (внешний ключ с таблицей users и полем iduser)
 idgroup int (внешний ключ с таблицей groups и полем idgroup)
 idsubject int (внешний ключ с таблицей subjects и полем idsubject)
 score int	- сама оценка
```
удаление - просто отметка об удалении ("При удалении данных они не должны теряться, а храниться для просмотра и восстановления")

### Домашнее задание  

Создать БД journal и добавить поле `deleted int (default 0) - отметка об удалении` в каждую таблицу   
Сделить дамп БД (Экспорт) и принести его на следующее занятие **в пятницу 30-05-25 (10:00)**  

# Занятие 2 (30-05-25) Просмотр оценок для студента

Установить проект с гитхаба WebBasicProject (кнонировать через гит, установить зависимости чере композер)   
1. Создать меню "Оценки" (BaseTemplate, '/scores')  
2. добавить маршрут '/scores' в Router  
3. добавить контроллер Score  
4. добавить модель ScoreDBStorage
5. добавить представление ScoreTemplate

делать - копирую существующие модель, контроллер, представление - от User

# Занятие 3 (03-06-25) Просмотр оценок для преподавателя

1. Создать меню "Оценки" (BaseTemplate, '/scores')  
2. добавить маршрут '/scores' в Router  
3. добавить контроллер Score  
4. добавить модуль ScoreDBStorage

# Занятие 4 (05-06-25) Просмотр дисциплин для преподавателя

1. Создать меню "Дисциплины" (BaseTemplate, '/subjects')  
2. добавить маршрут '/subjects' в Router  
3. добавить контроллер Subjects  
4. добавить модуль SubjectsDBStorage  
6. добавить представление SubjectsTemplate  



